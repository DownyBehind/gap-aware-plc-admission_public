# Tests

Purpose: project-level wrappers for ns-3 build/tests, Python syntax checks, and structure validation.

Main files: `run_cpp_tests.sh`, `run_python_tests.sh`, `run_all_tests.sh`.

Use: run `./tests/run_all_tests.sh` from this project root.

Related tests: ns-3 C++ tests live in `ns-3-dev/contrib/ev-plc-transition/test`.

Expected outputs: passing command output and optional smoke-test result files.
